// representation of space on the memory
// wait to deal with data on the memory in a fast way
// store data as binary
// js can't work well nativelly with binary data
// buffer came to deal with that

const buf = Buffer.from("ok");

console.log(buf);